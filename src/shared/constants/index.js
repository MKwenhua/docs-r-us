import {
  CDN_URI
} from './resources'

const CONNECTED = 'CONNECTED';
const CHANGE_VIEW = 'CHANGE_VIEW';
const DISCONNECTED = 'DISCONNECTED';

export {
  CONNECTED,
  CHANGE_VIEW,
  DISCONNECTED,
  CDN_URI
}
